syntax = "proto2";
package p2pnet.net;

message UDPSocketDestination_s {
	required string ip = 1;
	optional uint32 port = 2;
}

message PeerRoute_s {
	repeated bytes dest_indir = 1;
	repeated bytes dest_udp = 2;
}